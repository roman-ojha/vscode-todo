export const __prod__ = process.env.NODE_ENV === "production";
// this is kind of the standard convention in nodejs to passin an environment variable NODE_ENV to tell in if we are running in produciton
